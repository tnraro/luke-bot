{
  "tasks": {
    "dev": "deno run --watch src/main.ts",
    "compile": "deno compile --allow-env --allow-read --allow-net --output dist/luke src/main.ts",
    "start": "deno run --allow-env --allow-read --allow-net src/main.ts"
  }
}
